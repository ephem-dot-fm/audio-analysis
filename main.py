import schedule
from yellow import run_threaded
import time

if __name__ == "__main__":
    print('aloha')
    # file_duration = 20
    # schedule.every(file_duration).seconds.do(
    #     run_threaded, station_name='SOMACLQ', file_duration=file_duration)

    # while True:
    #     schedule.run_pending()
    #     time.sleep(1)
